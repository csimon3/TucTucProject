<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.solucionjava.tuctuc"
    android:versionCode="1"
    android:versionName="1.1" >

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-sdk
        android:minSdkVersion="7"
        android:targetSdkVersion="19" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme"

        >
        android:configChanges="keyboardHidden|orientation"
        <activity
            android:name="com.solucionjava.tuctuc.MainActivity"
            android:configChanges="keyboard|keyboardHidden|mcc|mnc|orientation|screenSize|fontScale|layoutDirection|locale|navigation|screenLayout|smallestScreenSize|touchscreen|uiMode"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.solucionjava.tuctuc.Vecinos"
            android:configChanges="keyboard|keyboardHidden|mcc|mnc|orientation|screenSize|fontScale|layoutDirection|locale|navigation|screenLayout|smallestScreenSize|touchscreen|uiMode"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
